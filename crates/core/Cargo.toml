[package]
name = "core"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
arena = { path = "../arena" }
diag = { path = "../diag" }
syntax = { path = "../syntax" }

[dev-dependencies]
types = { path = "../types" }
