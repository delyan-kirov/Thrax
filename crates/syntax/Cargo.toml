[package]
name = "syntax"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
diag = { path = "../diag" }
arena = { path = "../arena" }
lexer = { path = "../lexer" }
